<ul>
    <li ng-repeat="p in periods | orderBy:'-'"
        ng-controller="PeriodCtrl as pd"
        ng-init="pd.init(p)">
        
        <article>
            <h1>{{ p | date:'MMM yyyy' }}</h1>
            
            <table class="full-width">
                <tr ng-repeat="c in pd.costs">
                    <td>{{ c.date | date:'dd' }}</td>
                    <td>{{ c.category.words }}</td>
                    <td>{{ c.amount }}</td>
                </tr>
            </table>
        </article>
    </li>
</ul>